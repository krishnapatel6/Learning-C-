#include<stdio.h>

int main(){
    int num;
    // Q.3 Write a program to determine whether a number is divisible by 97 or not
    printf ("Enter the number\n");
    scanf (" %d ", &num);
    printf (" Divisibility test retuns: %d\n", num%97);
    return 0;

    Q.4 3*x/y-z + k
     int x = 2,y = 3, z =3, k = 1;

}